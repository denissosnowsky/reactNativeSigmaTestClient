import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapepr: {
    height: 90,
    width: 180,
    display: 'flex',
    alignItems: 'center',
    marginTop: 50,
    marginBottom: 20,
  },
  image: {
    height: '100%',
    resizeMode: 'contain',
  },
});

export default styles;
